# Local Build Artifact Overrides

This directory allows one to override the default build artifacts when running
XTS in local mode. Each subdirectory can be used to override the corresponding
artifact by copying the appropriate .zip file into it.
