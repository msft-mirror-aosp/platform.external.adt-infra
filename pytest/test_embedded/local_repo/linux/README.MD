This directory contains linux specific bindings.

It mainly contains a collection of shared libraries that were used to
compile the python wheels. This will make sure that we have the
right dependencies available, v.s. the system ones.
